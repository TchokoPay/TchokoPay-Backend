export enum FlowType {
  DIRECT = 'DIRECT',
  QR = 'QR',
  REQUEST = 'REQUEST',
}

export enum PaymentAction {
  CREATE = 'CREATE',
  PAY = 'PAY',
}

export enum CurrencyCode {
  BTC = 'BTC',
  SAT = 'SAT',
  XAF = 'XAF',
  USDT = 'USDT',
}

export enum AmountType {
  PAY = 'PAY',
  RECEIVE = 'RECEIVE',
}